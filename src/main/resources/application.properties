spring.application.name=moneto-backend
# Server
server.port=8080

# Database PostgreSQL
spring.datasource.url=jdbc:postgresql://aws-1-sa-east-1.pooler.supabase.com:5432/postgres
spring.datasource.username=postgres.fpwampyjgliucgqmdzsa
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# JWT
jwt.secret=${JWT_SECRET:}
jwt.expiration=86400000
jwt.refresh-expiration=604800000

# CORS
spring.web.cors.allowed-origins=http://localhost:5173

# Security headers
server.servlet.session.cookie.secure=true
server.servlet.session.cookie.http-only=true

# Logging (sem dados sensíveis)
logging.level.org.springframework.security=WARN
logging.level.com.moneto=INFO

# WhatsApp Cloud API
whatsapp.api.url=https://graph.facebook.com/v18.0
whatsapp.phone.number.id=1123706594153936
whatsapp.access.token=${WHATSAPP_ACCESS_TOKEN:}
whatsapp.verify.token=moneto_2026

# OpenAI
openai.api.key=${OPENAI_API_KEY:}
openai.model=gpt-4o-mini